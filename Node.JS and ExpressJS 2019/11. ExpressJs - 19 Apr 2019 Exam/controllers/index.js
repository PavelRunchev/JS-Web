const home = require('./home-controller');
const user = require('./user-controller');
const course = require('./course-controller');
const lecture = require('./lecture-controllers');

module.exports = {
    home,
    user,
    course,
    lecture
};